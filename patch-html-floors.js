const fs = require('fs');
let html = fs.readFileSync('index.html', 'utf8');

// The reel is empty in HTML (populated by JS), but let's make sure it's 0-6 in JS!
// So nothing needed in index.html specifically if it's generated by JS.
console.log('Checked HTML floor reel (handled in JS)');
